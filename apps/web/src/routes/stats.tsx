import { ComingSoon } from "../components/ComingSoon";

export function StatsRoute() {
  return (
    <ComingSoon
      eyebrow="Stats"
      title="Nothing to chart yet."
      body="Play a session, and this is where the numbers — and the story behind them — will live."
    />
  );
}
