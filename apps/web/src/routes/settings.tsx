import { ComingSoon } from "../components/ComingSoon";

export function SettingsRoute() {
  return (
    <ComingSoon
      eyebrow="Settings"
      title="Nothing to configure yet."
      body="Sound, skins, and table preferences are coming — the defaults are doing fine for now."
    />
  );
}
