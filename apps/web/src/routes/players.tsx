import { ComingSoon } from "../components/ComingSoon";

export function PlayersRoute() {
  return (
    <ComingSoon
      eyebrow="Players"
      title="No book on anyone yet."
      body="Reads, tells, and history on the bots you've played build up here — you haven't sat with anyone yet."
    />
  );
}
